<!doctype html>
<html lang="ar" dir="rtl">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>مِحَك · TrustLens — لوحة الذكاء التشغيلي</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800&display=swap" rel="stylesheet">
<style>
  :root{
    --navy:#1e1e42; --navy2:#2a2a58; --terra:#c26b4f; --purple:#8785d8;
    --bg:#eef0f4; --card:#ffffff; --ink:#1c2440; --muted:#6c7488; --line:#e7e9ef;
    --green:#2f9e6b; --amber:#dd9a34; --red:#d1564c;
    --shadow:0 10px 30px rgba(30,30,66,.08), 0 2px 8px rgba(30,30,66,.05);
  }
  *{box-sizing:border-box}
  html,body{margin:0}
  body{
    font-family:'Tajawal','Segoe UI',Tahoma,sans-serif;
    background:radial-gradient(1200px 600px at 90% -10%, #23234f 0%, #1a1a3c 40%, #15152f 100%);
    color:var(--ink); min-height:100vh; padding:22px;
  }
  .wrap{max-width:1180px;margin:0 auto}

  /* Top bar */
  .topbar{display:flex;align-items:center;justify-content:space-between;gap:18px;
    padding:14px 20px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.09);
    border-radius:18px;backdrop-filter:blur(6px)}
  .brand{display:flex;align-items:center;gap:12px;color:#fff}
  .brand .mark{width:42px;height:42px;border-radius:12px;background:linear-gradient(135deg,var(--purple),var(--terra));
    display:grid;place-items:center;font-weight:800;color:#fff;font-size:20px;box-shadow:0 6px 18px rgba(135,133,216,.45)}
  .brand h1{font-size:20px;margin:0;font-weight:800;letter-spacing:.2px}
  .brand span{display:block;font-size:11px;color:#b9b9d6;font-weight:500;letter-spacing:2px}
  .search{display:flex;gap:10px;align-items:center;flex:1;max-width:520px}
  .search input{flex:1;padding:12px 16px;border-radius:12px;border:1px solid rgba(255,255,255,.14);
    background:rgba(255,255,255,.95);font-family:inherit;font-size:15px;color:var(--ink);outline:none}
  .search input::placeholder{color:#9aa0b3}
  .btn{padding:12px 22px;border:0;border-radius:12px;background:var(--terra);color:#fff;font-family:inherit;
    font-weight:700;font-size:15px;cursor:pointer;white-space:nowrap;transition:transform .12s, filter .12s}
  .btn:hover{filter:brightness(1.06)}.btn:active{transform:scale(.97)}
  .sources{display:flex;gap:8px;align-items:center}
  .src{font-size:11px;color:#cfd0e6;background:rgba(255,255,255,.08);border:1px solid rgba(255,255,255,.1);
    padding:6px 10px;border-radius:999px;display:flex;align-items:center;gap:6px;font-weight:500}
  .dot{width:7px;height:7px;border-radius:50%;background:var(--green);box-shadow:0 0 8px var(--green)}

  /* company chips */
  .chips{display:flex;gap:10px;margin:16px 2px 20px;flex-wrap:wrap}
  .chip{padding:9px 16px;border-radius:999px;border:1px solid rgba(255,255,255,.16);
    background:rgba(255,255,255,.06);color:#dcdcf0;font-family:inherit;font-size:13.5px;font-weight:500;
    cursor:pointer;transition:.15s}
  .chip:hover{background:rgba(255,255,255,.13)}
  .chip.active{background:#fff;color:var(--navy);border-color:#fff;font-weight:700}
  .chip small{opacity:.6;font-weight:500;margin-inline-start:6px}

  /* grid */
  .grid{display:grid;grid-template-columns:360px 1fr;gap:18px}
  .card{background:var(--card);border-radius:20px;box-shadow:var(--shadow);padding:22px;border:1px solid var(--line)}
  .card h2{font-size:14px;margin:0 0 4px;color:var(--muted);font-weight:700;letter-spacing:.3px}
  .col{display:flex;flex-direction:column;gap:18px}

  /* gauge */
  .gaugecard{display:flex;flex-direction:column;align-items:center;text-align:center}
  .company-name{font-size:19px;font-weight:800;color:var(--navy);margin:2px 0 2px}
  .company-cr{font-size:12px;color:var(--muted);margin-bottom:12px}
  .gauge{position:relative;width:220px;height:220px}
  .gauge svg{transform:rotate(-90deg)}
  .gauge .val{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center}
  .gauge .num{font-size:56px;font-weight:800;line-height:1;color:var(--navy)}
  .gauge .den{font-size:14px;color:var(--muted);font-weight:600;margin-top:2px}
  .verdict{margin-top:14px;font-size:15px;font-weight:700;padding:8px 18px;border-radius:999px}
  .sectorbar{width:100%;margin-top:18px}
  .sectorbar .lbl{display:flex;justify-content:space-between;font-size:12px;color:var(--muted);margin-bottom:6px}
  .track{position:relative;height:8px;background:#eef0f5;border-radius:999px}
  .track .fill{position:absolute;inset-inline-start:0;top:0;bottom:0;border-radius:999px;background:var(--navy);width:0;transition:width 1s ease}
  .track .sector{position:absolute;top:-3px;width:2px;height:14px;background:var(--terra);border-radius:2px}
  .track .sector::after{content:"القطاع";position:absolute;top:16px;font-size:10px;color:var(--terra);inset-inline-start:-14px;white-space:nowrap;font-weight:700}

  /* recommendation */
  .rechead{display:flex;align-items:center;gap:10px;margin-bottom:12px}
  .rec-verdict{font-size:22px;font-weight:800}
  .rec-row{display:flex;justify-content:space-between;padding:11px 0;border-top:1px solid var(--line);font-size:14px}
  .rec-row span:first-child{color:var(--muted)}
  .rec-row span:last-child{font-weight:700}
  .risk-pill{padding:3px 12px;border-radius:999px;font-size:12.5px;font-weight:700}

  /* indicators */
  .ind{margin:14px 0}
  .ind .top{display:flex;justify-content:space-between;font-size:13.5px;margin-bottom:7px}
  .ind .top b{font-weight:700;color:var(--ink)} .ind .top i{font-style:normal;font-weight:700}
  .ind .bar{height:9px;background:#eef0f5;border-radius:999px;overflow:hidden}
  .ind .bar>div{height:100%;width:0;border-radius:999px;transition:width .9s cubic-bezier(.22,1,.36,1)}

  /* alerts */
  .alert{display:flex;gap:12px;padding:13px;border-radius:14px;background:#f8f9fb;margin-bottom:10px;border:1px solid var(--line)}
  .alert .tag{flex:0 0 auto;align-self:flex-start;font-size:11px;font-weight:800;padding:4px 10px;border-radius:8px;color:#fff}
  .alert .body{flex:1}
  .alert .txt{font-size:14px;font-weight:600;color:var(--ink);line-height:1.5}
  .alert .meta{font-size:11.5px;color:var(--muted);margin-top:4px;display:flex;gap:10px}
  .alert .meta b{color:var(--terra);font-weight:700}

  /* report */
  .report .body{font-size:14.5px;line-height:1.9;color:#374151;background:#faf9f7;border-radius:14px;padding:18px 20px;border:1px dashed #e4d9d2}
  .report .sig{margin-top:12px;font-size:12px;color:var(--muted)}
  .ai{font-size:11px;color:var(--purple);font-weight:700;background:#f0effc;padding:4px 10px;border-radius:8px}

  .fade{opacity:0;transform:translateY(10px);animation:rise .55s forwards}
  @keyframes rise{to{opacity:1;transform:none}}
  @media (max-width:820px){.grid{grid-template-columns:1fr}}
  @media (prefers-reduced-motion:reduce){*{animation:none!important;transition:none!important}}
</style>
</head>
<body>
<div class="wrap">

  <div class="topbar">
    <div class="brand">
      <div class="mark">م</div>
      <div><h1>مِحَك</h1><span>TRUSTLENS</span></div>
    </div>
    <div class="search">
      <input id="cr" placeholder="أدخل رقم السجل التجاري…" value="1010574320">
      <button class="btn" onclick="analyze()">تحليل</button>
    </div>
    <div class="sources">
      <div class="src"><span class="dot"></span>GOSI</div>
      <div class="src"><span class="dot"></span>ZATCA</div>
      <div class="src"><span class="dot"></span>مدد</div>
      <div class="src"><span class="dot"></span>Open Banking</div>
      <div class="src"><span class="dot"></span>SIMAH</div>
    </div>
  </div>

  <div class="chips" id="chips"></div>

  <div class="grid">
    <!-- Right column: gauge + recommendation -->
    <div class="col">
      <div class="card gaugecard" id="gaugeCard">
        <div class="company-name" id="cname">—</div>
        <div class="company-cr" id="ccr">—</div>
        <div class="gauge">
          <svg width="220" height="220" viewBox="0 0 220 220">
            <circle cx="110" cy="110" r="95" fill="none" stroke="#eef0f5" stroke-width="18"/>
            <circle id="arc" cx="110" cy="110" r="95" fill="none" stroke="#2f9e6b" stroke-width="18"
              stroke-linecap="round" stroke-dasharray="597" stroke-dashoffset="597"
              style="transition:stroke-dashoffset 1.1s cubic-bezier(.22,1,.36,1), stroke .6s"/>
          </svg>
          <div class="val"><div class="num" id="score">0</div><div class="den">درجة الثقة التشغيلية</div></div>
        </div>
        <div class="verdict" id="verdict">—</div>
        <div class="sectorbar">
          <div class="lbl"><span>مقارنة بمتوسط القطاع</span><span id="sectorTxt"></span></div>
          <div class="track"><div class="fill" id="sfill"></div><div class="sector" id="smark"></div></div>
        </div>
      </div>

      <div class="card" id="recCard">
        <h2>توصية التمويل</h2>
        <div class="rechead"><div class="rec-verdict" id="recV">—</div></div>
        <div class="rec-row"><span>مستوى المخاطرة</span><span><span class="risk-pill" id="risk">—</span></span></div>
        <div class="rec-row"><span>الحد المقترح</span><span id="limit">—</span></div>
        <div class="rec-row"><span>موعد إعادة التقييم</span><span id="review">—</span></div>
      </div>
    </div>

    <!-- Left column: indicators + alerts + report -->
    <div class="col">
      <div class="card" id="indCard">
        <h2>تحليل المؤشرات</h2>
        <div id="inds"></div>
      </div>

      <div class="card" id="alertCard">
        <h2>التنبيهات المبكرة</h2>
        <div id="alerts"></div>
      </div>

      <div class="card report" id="reportCard">
        <h2 style="display:flex;justify-content:space-between;align-items:center">
          <span>التقرير الذكي</span><span class="ai">مُولّد بالذكاء الاصطناعي</span></h2>
        <div class="body" id="report">—</div>
        <div class="sig" id="sig"></div>
      </div>
    </div>
  </div>
</div>

<script>
const IND = ["استقرار القوى العاملة","الالتزام بصرف الرواتب","صحة الإيراد الفعلي","صحة التدفق النقدي","السجل الائتماني"];
const SECTOR = 65;

const DB = {
  "1010574320": {
    name:"مؤسسة الأفق للمقاولات", score:82, verdict:["منشأة سليمة","#2f9e6b"],
    ind:[88,90,78,80,84],
    rec:{v:["موافقة","#2f9e6b"], risk:["منخفضة","#2f9e6b","#eaf7f0"], limit:"حتى ٢٫٥ مليون ريال", review:"بعد ١٢ شهراً"},
    alerts:[["تنبيه","#8785d8","تذبذب طفيف في التدفق النقدي خلال الربع الأخير ضمن الحدود الطبيعية","Open Banking","قبل ٩ أيام"]],
    report:"تُظهر بيانات المنشأة استقراراً تشغيلياً قوياً: قوى عاملة ثابتة والتزام منتظم بصرف الرواتب عبر نظام مدد، مع نمو مطّرد في الإيراد المُقرّ لدى هيئة الزكاة والضريبة. درجة الثقة (٨٢) تفوق متوسط القطاع (٦٥)، ولا توجد إشارات خطر جوهرية. يُوصى بالموافقة على التمويل بمخاطرة منخفضة."
  },
  "1010889077": {
    name:"شركة نجمة الخليج التجارية", score:58, verdict:["تحتاج مراقبة","#dd9a34"],
    ind:[62,55,60,52,61],
    rec:{v:["تمويل جزئي","#dd9a34"], risk:["متوسطة","#dd9a34","#fbf3e3"], limit:"حتى ٦٠٠ ألف ريال", review:"بعد ٦ أشهر"},
    alerts:[
      ["تحذير","#dd9a34","تأخّر صرف رواتب شهر واحد لجزء من الموظفين","مدد / WPS","قبل ٢١ يوماً"],
      ["تنبيه","#8785d8","تراجع الإيراد المُقرّ بنسبة ١٢٪ مقارنة بالربع السابق","ZATCA","قبل شهر"]
    ],
    report:"ترصد المنصة إشارات ضغط تشغيلي مبكرة: تأخّر في صرف رواتب شهر عبر نظام مدد، مصحوباً بتراجع في الإيراد المُقرّ لدى هيئة الزكاة والضريبة بنحو ١٢٪. القوى العاملة لا تزال مستقرة نسبياً. درجة الثقة (٥٨) دون متوسط القطاع (٦٥). يُوصى بتمويل جزئي مع مخاطرة متوسطة وإعادة تقييم خلال ستة أشهر."
  },
  "1010233918": {
    name:"مصنع البيان للتعبئة والتغليف", score:34, verdict:["مخاطرة مرتفعة","#d1564c"],
    ind:[30,25,40,28,45],
    rec:{v:["تأجيل التمويل","#d1564c"], risk:["مرتفعة","#d1564c","#faeae8"], limit:"غير موصى به حالياً", review:"بعد ٣ أشهر"},
    alerts:[
      ["خطر","#d1564c","تسريح ٢٢٪ من الموظفين خلال ربع واحد","GOSI","قبل ١١ يوماً"],
      ["خطر","#d1564c","تأخّر صرف الرواتب لشهرين متتاليين","مدد / WPS","قبل ٦ أيام"],
      ["تحذير","#dd9a34","انكماش الإيراد المُقرّ بنسبة ٣١٪ خلال عام","ZATCA","قبل شهرين"]
    ],
    report:"تتقاطع عدة إشارات خطر تسبق ظهور التعثّر في القوائم المالية: تقلّص حاد في عدد المشتركين لدى التأمينات الاجتماعية (تسريح ٢٢٪)، وتأخّر صرف الرواتب لشهرين عبر مدد، وانكماش الإيراد المُقرّ بنسبة ٣١٪. درجة الثقة (٣٤) أقل بكثير من متوسط القطاع (٦٥). يُوصى بتأجيل التمويل وإعادة التقييم خلال ثلاثة أشهر."
  }
};

const chips = document.getElementById('chips');
Object.entries(DB).forEach(([cr,d],i)=>{
  const c=document.createElement('button');
  c.className='chip'+(i===0?' active':'');
  c.innerHTML = d.name+' <small>'+d.verdict[0]+'</small>';
  c.onclick=()=>{document.getElementById('cr').value=cr;analyze(c);};
  chips.appendChild(c);
});

function colorFor(v){return v>=70?'#2f9e6b':v>=50?'#dd9a34':'#d1564c';}

function analyze(chipEl){
  const cr=document.getElementById('cr').value.trim();
  const d=DB[cr]||DB["1010574320"];
  document.querySelectorAll('.chip').forEach(c=>c.classList.remove('active'));
  if(chipEl) chipEl.classList.add('active');
  else {const idx=Object.keys(DB).indexOf(cr); if(idx>=0) chips.children[idx].classList.add('active');}

  // fade cards
  ['gaugeCard','recCard','indCard','alertCard','reportCard'].forEach((id,i)=>{
    const el=document.getElementById(id); el.classList.remove('fade'); void el.offsetWidth;
    el.style.animationDelay=(i*70)+'ms'; el.classList.add('fade');
  });

  document.getElementById('cname').textContent=d.name;
  document.getElementById('ccr').textContent='سجل تجاري: '+cr;

  // gauge
  const arc=document.getElementById('arc'), C=597;
  arc.style.strokeDashoffset=C; arc.style.stroke=colorFor(d.score);
  const num=document.getElementById('score');
  const vd=document.getElementById('verdict');
  vd.textContent=d.verdict[0]; vd.style.color=d.verdict[1]; vd.style.background=d.verdict[1]+'1a';
  setTimeout(()=>{ arc.style.strokeDashoffset=C-(C*d.score/100);
    let n=0; const step=Math.max(1,Math.round(d.score/38));
    const t=setInterval(()=>{n+=step; if(n>=d.score){n=d.score;clearInterval(t);} num.textContent=n; num.style.color=colorFor(d.score);},22);
  },120);

  // sector bar
  document.getElementById('sectorTxt').textContent=d.score+' مقابل '+SECTOR;
  document.getElementById('smark').style.insetInlineStart=SECTOR+'%';
  setTimeout(()=>{const f=document.getElementById('sfill');f.style.width=d.score+'%';f.style.background=colorFor(d.score);},160);

  // recommendation
  const r=d.rec;
  const rv=document.getElementById('recV'); rv.textContent=r.v[0]; rv.style.color=r.v[1];
  const rk=document.getElementById('risk'); rk.textContent=r.risk[0]; rk.style.color=r.risk[1]; rk.style.background=r.risk[2];
  document.getElementById('limit').textContent=r.limit;
  document.getElementById('review').textContent=r.review;

  // indicators
  const inds=document.getElementById('inds'); inds.innerHTML='';
  d.ind.forEach((v,i)=>{
    const el=document.createElement('div'); el.className='ind';
    el.innerHTML=`<div class="top"><b>${IND[i]}</b><i style="color:${colorFor(v)}">${v}</i></div>
      <div class="bar"><div style="background:${colorFor(v)}"></div></div>`;
    inds.appendChild(el);
    setTimeout(()=>{el.querySelector('.bar>div').style.width=v+'%';},180+i*80);
  });

  // alerts
  const al=document.getElementById('alerts'); al.innerHTML='';
  d.alerts.forEach(a=>{
    const el=document.createElement('div'); el.className='alert';
    el.innerHTML=`<span class="tag" style="background:${a[1]}">${a[0]}</span>
      <div class="body"><div class="txt">${a[2]}</div>
      <div class="meta"><span>المصدر: <b>${a[3]}</b></span><span>${a[4]}</span></div></div>`;
    al.appendChild(el);
  });

  // report
  document.getElementById('report').textContent=d.report;
  document.getElementById('sig').textContent='صيغ آلياً من بيانات حكومية موثّقة · '+new Date().toLocaleDateString('ar-SA');
}
analyze(chips.children[0]);
</script>
</body>
</html>